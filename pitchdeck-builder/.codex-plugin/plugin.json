{
  "name": "pitchdeck-builder",
  "version": "0.1.0",
  "description": "Build a responsive, interactive pitch-deck website from a brand book + a PRD / example deck. Mirrors brandbook-builder's brief → build → QA/deploy flow but emits a section-scroll Vite + React + TS + Tailwind + motion deck site.",
  "skills": [
    "skills/pitchdeck"
  ],
  "scripts": {
    "validate": "scripts/validate-pitchdeck-project.mjs"
  }
}
